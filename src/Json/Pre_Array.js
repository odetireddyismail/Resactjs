import React from 'react'

const Pre_Array = (p) => {
    //console.log(p)
  return (
    <div>Pre_Array
        <h1>{p.name[0].firstName}</h1>
        <h1>{p.name[0].lastName}</h1>
    </div>
  )
}

export default Pre_Array